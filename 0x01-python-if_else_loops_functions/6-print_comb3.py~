#!/usr/bin/python3
for a in range(0, 9):
    for b in range(1, 9):
        if a == b or a > b:
            continue
        else:
            if (a == 8): print("89")
            else: print(f"{a}{b}", end=", ")
